apiVersion: apps/v1
kind: Deployment
metadata:
  name: malscan-api
  namespace: malscan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: malscan-api
  template:
    metadata:
      labels:
        app: malscan-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: api
          image: ghcr.io/OWNER/malscan-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: malscan-config
            - secretRef:
                name: malscan-secrets
          env:
            - name: CORS_ORIGINS
              value: "https://OWNER.github.io"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
      volumes:
        - name: tmp-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: malscan-api
  namespace: malscan
spec:
  type: NodePort
  selector:
    app: malscan-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
